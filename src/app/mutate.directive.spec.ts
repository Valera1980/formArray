import { MutateDirective } from './mutate.directive';

describe('MutateDirective', () => {
  it('should create an instance', () => {
    const directive = new MutateDirective();
    expect(directive).toBeTruthy();
  });
});
